export default interface formDataType {
    pass: string,
    cpass: string
};

export default interface payloadType {
    pass: string;
    cpass: string;
    username: string | null;
    Code: string;
};
